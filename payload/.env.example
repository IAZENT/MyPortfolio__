# Example env values for Payload
# Database (Postgres recommended for Payload full-text search)
MONGODB_URI=mongodb://localhost:27017/payload

# Required by Payload for signing cookies/sessions
PAYLOAD_SECRET=change_this_to_a_strong_secret

# URL where Payload will be reachable (for preview links & webhooks)
PAYLOAD_SERVER_URL=http://localhost:3001

# Cloudinary (optional) - add only after creating account
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME

# (Optional) External services for PDF parsing, Notion sync etc.
PDF_EXTRACTOR_URL=
NOTION_CLIENT_ID=
NOTION_CLIENT_SECRET=

# Dev convenience: initial admin credentials (change before production)
ADMIN_EMAIL=admin@localhost.local
ADMIN_PASSWORD=changeme
